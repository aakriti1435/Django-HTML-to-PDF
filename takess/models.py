from django.db import models
from django import forms

# Create your models here.


# class screen(models.Model):
#     signature = models.ImageField(upload_to='images/', widget=forms.HiddenInput(),null=True)